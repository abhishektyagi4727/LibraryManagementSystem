[phases.setup]
commands = ["echo 'Setup phase - Java only'"]

[phases.install]
commands = ["echo 'No dependencies needed'"]

[phases.build]
commands = ["echo 'Skipping build - WAR will be run directly'"]

[phases.start]
commands = ["sh start.sh"]

[env]
JAVA_VERSION = "17"
