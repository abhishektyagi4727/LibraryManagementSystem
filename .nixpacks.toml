[phases.setup]
commands = ["echo 'Setup phase: installing Java 17'"]

[phases.install]
commands = ["echo 'No extra dependencies needed'"]

[phases.build]
commands = ["echo 'Skipping build - WAR will run directly'"]

[phases.start]
commands = ["sh start.sh"]

[env]
JAVA_VERSION = "17"
